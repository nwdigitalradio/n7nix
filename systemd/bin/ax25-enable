#!/bin/bash
# Be sure we're running as root
if [[ $EUID != 0 ]] ; then
   echo "Must be root"
   exit 1
fi
systemctl enable ax25dev.service
systemctl enable ax25dev.path
systemctl enable direwolf.service
systemctl enable ax25-mheardd.service
systemctl enable ax25d.service
